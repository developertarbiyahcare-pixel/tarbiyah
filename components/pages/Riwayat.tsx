import React from 'react';

const Riwayat: React.FC = () => {
    // This component's content has been moved to LaporanSpiritual.tsx
    // and is no longer routed. Returning null to make it an empty component.
    return null;
};

export default Riwayat;